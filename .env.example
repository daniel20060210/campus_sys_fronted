# 环境变量示例文件
# 本地开发测试用到 docker compose 容器时，请先在根目录创建名为 ".env" 的文件
# 填入本文件的内容，在其中补全以星号代替的变量，必要时修改示例提供的默认值

# 管理端统一后端前缀：API、图片和下载资源都会使用此地址。
# 本地留空时使用 Vite 代理；线上可填写 HTTPS 地址或 /backend 这类同源路径。
VITE_BACKEND_PREFIX=

# [本地开发] 管理端 宿主机端口
CB_MANAGE_LOCAL_PORT=36664
